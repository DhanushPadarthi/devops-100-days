name: 🚀 Day 4 - Node CI Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a greeting script
        run: echo "👋 Hello from GitHub Actions!"
